import { ProductoPipePipe } from './producto-pipe.pipe';

describe('ProductoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
